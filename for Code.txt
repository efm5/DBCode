Anything that goes wrong in: resource loading; menu construction; icon creation; UI initialization
…will surface in one predictable place, with a message you control. 
If you want to take it one notch further later on, we can add:
a “resource sanity check” method that validates all embedded icons before the UI builds
or a small logging hook so fatal errors get written to a text file before exit

My general practice would be to do a small logging hook so fatal errors get written to a text file before exit. But I will add that polish after everything is working. I have kept a reminder to do so. I also typically do a “resource sanity check” method that validates all embedded icons before the UI builds – but again for later.